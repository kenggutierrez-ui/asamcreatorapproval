[build]
  publish = "."
    functions = "netlify/functions"

    [functions]
      timeout = 26

      [[redirects]]
        from = "/api/claude"
          to = "/.netlify/functions/claude"
            status = 200
            
